# Second-python-project-06
"A Python script that demonstrates how to calculate the square of a number using different methods, while highlighting the common mistake of using the XOR operator (^) instead of the exponentiation operator (**)."
